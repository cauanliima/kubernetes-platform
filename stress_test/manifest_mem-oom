apiVersion: v1
kind: Pod
metadata:
  name: oom-test
spec:
  containers:
  - name: oom
    image: python:3.11-slim
    command: ["python", "-u", "-c", "a=[]\nwhile True:\n a.append(' ' * 1000000)"]
    resources:
      limits:
        memory: "100Mi"
      requests:
        memory: "50Mi"
